<h1>Recent Posts</h1>
{{#each posts}}
<div class="card mt-4">
    <div class="card-body">
        <h3>{{title}}</h3>
        <p>{{description}}</p>
        <a href="/post/{{slug}}"><button class="btn btn-info">Read More</button></a>
        <hr>
        <small>In {{category.name}}</small>
        <small>At {{date}}</small>
        <small>by BL#00001</small>
    </div>
</div>
{{else}}
<h5>No Posts</h5>
{{/each}}